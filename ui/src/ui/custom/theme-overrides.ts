// Optional overlay-level theme tweaks.
//
// Keep custom global styling here so upstream theme changes merge cleanly.
// If you need to override a CSS variable, prefer setting it on :root.

export {};

